// A part of the C# Language Syntactic Sugar suite.

namespace CLSS
{
  public static partial class CommonMathOps
  {
    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Remainder of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static int Remainder(this sbyte source, int divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static uint Remainder(this byte source, uint divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static int Remainder(this short source, int divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static uint Remainder(this ushort source, uint divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static int Remainder(this int source, int divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static uint Remainder(this uint source, uint divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static long Remainder(this sbyte source, long divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static ulong Remainder(this byte source, ulong divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static long Remainder(this short source, long divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static ulong Remainder(this ushort source, ulong divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static long Remainder(this int source, long divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static ulong Remainder(this uint source, ulong divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static long Remainder(this long source, long divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static ulong Remainder(this ulong source, ulong divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this sbyte source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this byte source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this short source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this ushort source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this int source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this uint source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this long source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this ulong source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static decimal Remainder(this decimal source, decimal divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this sbyte source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this byte source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this short source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this ushort source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this int source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this uint source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this long source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this ulong source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this sbyte source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this byte source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this short source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this ushort source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this int source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this uint source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this long source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this ulong source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this float source, int divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this float source, long divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this float source, ulong divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static float Remainder(this float source, float divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this float source, double divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this double source, int divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this double source, long divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this double source, ulong divisor)
    { return source % divisor; }

    /// <summary>
    /// Calculates the remainder of the division of source number by another
    /// number.
    /// </summary>
    /// <param name="source">The dividend in a division.</param>
    /// <param name="divisor">The divisor in a division.</param>
    /// <returns>Result of <paramref name="source"/> %
    /// <paramref name="divisor"/>.</returns>
    public static double Remainder(this double source, double divisor)
    { return source % divisor; }
  }
}
