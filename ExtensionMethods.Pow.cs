// A part of the C# Language Syntactic Sugar suite.

using System;

namespace CLSS
{
  public static partial class CommonMathOps
  {
    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this sbyte source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this byte source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this short source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this ushort source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this int source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this uint source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this long source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this ulong source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this float source, double power)
    { return Math.Pow(source, power); }

    /// <summary>
    /// Returns the source number raised to the specified power.
    /// </summary>
    /// <param name="source">A double-precision floating-point number to be
    /// raised to a power.</param>
    /// <param name="power">A double-precision floating-point number that specifies
    /// a power.</param>
    /// <returns>The same result returned by
    /// Math.Pow(<paramref name="source"/>, <paramref name="power"/>).</returns>
    public static double Pow(this double source, double power)
    { return Math.Pow(source, power); }
  }
}
