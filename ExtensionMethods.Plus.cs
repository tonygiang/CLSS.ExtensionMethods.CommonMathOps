// A part of the C# Language Syntactic Sugar suite.

namespace CLSS
{
  public static partial class CommonMathOps
  {
    /// <summary>
    /// Adds source number with another number.
    /// </summary>
    /// <param name="source">The first addend in an addition.</param>
    /// <param name="rightAddend">The second addend in an addition.</param>
    /// <returns>Result of <paramref name="source"/> +
    /// <paramref name="rightAddend"/>.</returns>
    public static int Plus(this sbyte source, int rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static uint Plus(this byte source, uint rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static int Plus(this short source, int rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static uint Plus(this ushort source, uint rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static int Plus(this int source, int rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static uint Plus(this uint source, uint rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static long Plus(this sbyte source, long rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static ulong Plus(this byte source, ulong rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static long Plus(this short source, long rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static ulong Plus(this ushort source, ulong rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static long Plus(this int source, long rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static ulong Plus(this uint source, ulong rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static long Plus(this long source, long rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static ulong Plus(this ulong source, ulong rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this sbyte source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this byte source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this short source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this ushort source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this int source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this uint source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this long source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this ulong source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static decimal Plus(this decimal source, decimal rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this sbyte source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this byte source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this short source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this ushort source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this int source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this uint source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this long source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this ulong source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this sbyte source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this byte source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this short source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this ushort source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this int source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this uint source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this long source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this ulong source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this float source, int rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this float source, long rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this float source, ulong rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static float Plus(this float source, float rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this float source, double rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this double source, int rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this double source, long rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this double source, ulong rightAddend)
    { return source + rightAddend; }

    /// <inheritdoc cref="Plus(sbyte, int)"/>
    public static double Plus(this double source, double rightAddend)
    { return source + rightAddend; }
  }
}
